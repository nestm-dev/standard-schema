---
'@nestm/standard-schema': patch
---

Add response-input DTO typing, an opt-in Nest CLI compiler plugin that infers native Standard Schema response metadata from explicit controller return types, and a runnable Nest CLI/Zod example that is verified against the packed package.
