---
'@nestm/standard-schema': patch
---

Add response-input DTO typing and an opt-in Nest CLI compiler plugin that infers native Standard Schema response metadata from explicit controller return types.
